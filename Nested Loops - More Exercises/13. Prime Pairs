using System;

namespace _13._Prime_Pairs
{
    class Program
    {
        static void Main(string[] args)
        {
            int initial1 = int.Parse(Console.ReadLine());
            int initial2 = int.Parse(Console.ReadLine());
            int diff1 = int.Parse(Console.ReadLine());
            int diff2 = int.Parse(Console.ReadLine());
           
            for (int i = initial1; i <= (initial1 + diff1); i++)
            {
                for (int j = initial2; j <= (initial2 + diff2); j++)
                {
                    if (i % 2 != 0 && i % 3 != 0 && i % 5 != 0 && i % 7 != 0 && j % 2 != 0 && j % 3 != 0 && j % 5 != 0 && j % 7 != 0)
                    {
                        Console.WriteLine($"{i}{j}");
                    }
                }
                                    
            }
                 
            

        }
    }
}
